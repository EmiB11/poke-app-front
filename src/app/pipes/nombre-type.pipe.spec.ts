import { NombreTypePipe } from './nombre-type.pipe';

describe('NombreTypePipe', () => {
  it('create an instance', () => {
    const pipe = new NombreTypePipe();
    expect(pipe).toBeTruthy();
  });
});
